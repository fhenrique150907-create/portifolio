PORT=4000
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=portfolio_db

# Para reaplicar o seed no PostgreSQL (apaga a tabela projects e reinsere os 4 projetos):
# npm run reset-seed
